'use strict';

describe('myApp.view_add_task module', function() {

  beforeEach(module('myApp.view_add_task'));

  describe('view_add_task controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});